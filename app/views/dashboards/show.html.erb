<h1>Dashboard!</h1>

<%= shout_form_for(TextShout) do |form| %>
  <% form.text_field :body, placeholder: "What's new?", required: true %>
<% end %>

<%= shout_form_for(PhotoShout) do |form| %>
  <%= form.file_field :photo, as: :file, required: true %>
<% end %>

<%= render @shouts %>