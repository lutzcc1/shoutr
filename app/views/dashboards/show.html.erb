<h1>Dashboard!</h1>

<%= form_for @shout do |f| %>
  <%= f.text_field :body, placeholder: "What's new?", required: true %>
  <%= f.submit "Shout!" %>

  
  <% @shouts.each do |shout| %>
      <div>
        <%= shout.body %>
      </div>
  <% end %>
  
<% end %>